services:
  db:
    image: postgres:17
    environment:
      - POSTGRES_PASSWORD=postgres
    ports:
      - 5432:5432
    volumes:
      - './tmp/db:/var/lib/postgres/data'

  # web:
  #   build: .
  #   command: bin/rails server --binding 0.0.0.0
  #   # env_file:
  #   #   - '.env'
  #   depends_on:
  #     - 'db'
  #   volumes:
  #     - '.:/app'
